export interface TypeButton {
    default: {
        background: string;
        text: string;
    };
    disabled: {
        background: string;
        text: string;
    };
}

export interface TypeSheet {
    background: string;
    handle: string;
}
